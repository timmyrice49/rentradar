__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local crawl state. The canonical database is restored from CI cache and
# backed up daily to data/ -- a working copy here should never be committed.
*.db
*.db-wal
*.db-shm

# ...except that daily backup, which is the entire disaster-recovery story.
!data/rentradar.db

/tmp/
